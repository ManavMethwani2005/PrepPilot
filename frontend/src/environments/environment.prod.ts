// Production environment configuration for PrepPilot
// When deploying to Render, set apiUrl to your Render backend service URL e.g. 'https://<your-service>.onrender.com/api'
export const environment = {
  production: true,
  apiUrl: 'https://preppilot-backend.onrender.com/api',
};
